const item = (state = {}, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

const items = (state = [], action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default items;
